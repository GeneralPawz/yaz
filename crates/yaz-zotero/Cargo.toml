[package]
name = "yaz-zotero"
description = "Zotero library access for yaz: Better BibTeX, local API, exported .bib, and sqlite."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true

[dependencies]
yaz-core.workspace = true
yaz-latex.workspace = true
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
camino.workspace = true
tracing.workspace = true
reqwest.workspace = true

# NOTE(phase-5): rusqlite is added for the offline source. It must be built with
# the bundled feature so it links natively on aarch64 (ADR-0014), and opened
# read-only on a copy — never against the live zotero.sqlite.

[dev-dependencies]
pretty_assertions.workspace = true
tempfile.workspace = true
insta.workspace = true
